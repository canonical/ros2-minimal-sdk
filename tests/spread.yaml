project: ros2-minimal

path: /home/ubuntu/project

backends:
  lxd:
    systems:
      - ubuntu-22.04:
          username: ubuntu
          vm: true

suites:
  main/:
    summary: Test the ros2-minimal SDK inside workshops
    environment:
      BASE: ubuntu@22.04
      ROS_DISTRO: humble
